ALGORITHM linked_list
VAR
    list :	STRUCT
                        data : STRING;
                        next : ^list;
                    END_STRUCT
    first : ^list := NULL ;

BEGIN
    set_instructions
END